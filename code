#include<reg51.h>
void Delay(unsigned int itime)
{	
	unsigned int i,j;
	for(i=0;i<itime;i++)
	{
		for(j=0;j<1275;j++);
	}
}
void main(void)
{
while(1)
	{	
	P1=0x00;
	Delay(15000);
	P1=0XFF;
	Delay(15000);
	}
}
